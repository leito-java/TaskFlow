spring:
  datasource:
    url: jdbc:h2:mem:taskflow-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    # Les tests rapides laissent Hibernate créer une base H2 isolée.
    enabled: false

app:
  cors:
    allowed-origins:
      - http://localhost:4200
